1. Navigate to Your Project Directory
Run the jpackage command from the UI module folder where the target directory is located:

powershell
Copy
cd D:\Student Record Management\ui
2. Verify the JAR Exists
Check if the JAR file srms-ui-1.0-SNAPSHOT.jar exists in the target folder:

powershell
Copy
ls target/  # Should list the JAR file
3. Rebuild the JAR (If Missing)
If the JAR is missing, rebuild it:

powershell
Copy
mvn clean package
4. Run jpackage Correctly
Use the absolute path to the JAR if needed:

powershell
Copy
jpackage `
  --input target/ `
  --name StudentManagement `
  --main-jar srms-ui-1.0-SNAPSHOT.jar `
  --main-class com.example.mysrms.ui.Application `
  --type app-image
Key Fixes
Run from the Correct Directory: Ensure you’re in the ui folder (where target exists).

Verify JAR Name: Match the JAR filename exactly (e.g., srms-ui-1.0-SNAPSHOT.jar).